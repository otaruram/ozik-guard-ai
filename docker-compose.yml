version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: oziksustain-backend
    restart: always
    ports:
      - "80:10000"
    env_file:
      - ./backend/.env
